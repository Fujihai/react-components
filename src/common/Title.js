export const Title = ({ children }) => (
  <h3
    style={{
      width: "100%",
      textAlign: "left",
      paddingBottom: 16,
      borderBottom: "1px solid #eeeeee",
    }}
  >
    {children}
  </h3>
);
